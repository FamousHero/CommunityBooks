from django.db import models

# Create your models here.


# Book - title, author, isbn, page count, location pk, status (available, checked-out, wishlist), cover image, version #, pub date

# User - email & password (email required to send notifs), phone optional, location pk

# Location - PK, location name (in case location name)